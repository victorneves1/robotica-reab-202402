string label              # Detected landmark class (e.g., "door", "table")
float32[] bbox            # Bounding box in format [x_min, y_min, x_max, y_max]
float32 confidence        # Detection confidence score